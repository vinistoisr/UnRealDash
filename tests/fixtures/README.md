# Shared fixtures

Synthetic recordings, frame bytes, valid and invalid documents and image data
belong here (PLAN.md 2.x and 3.x). Expected failures use .expected siblings.
The 16 by 16 image in images/lfs-probe.png exercises LFS tracking (1.2).
Test implementation code, captured private data and build artifacts do not belong here.
