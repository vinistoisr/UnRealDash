; Input bindings are defined in later player tasks.

[/Script/Engine.InputSettings]
; The engine adds virtual joysticks on touch platforms by default. A dashboard has no character to
; drive, and they draw over the display. Observed on the Pixel during the PLAN 4.0 smoke run.
DefaultTouchInterface=None
