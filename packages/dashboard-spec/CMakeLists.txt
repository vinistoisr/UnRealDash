cmake_minimum_required(VERSION 3.25)
project(dashboard_spec LANGUAGES C CXX)
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)
if(MSVC)
    string(REPLACE "/EHsc" "" CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS}")
    string(REPLACE "/GR" "" CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS}")
    string(REPLACE "/W3" "" CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS}")
endif()
add_library(miniz STATIC ../../third_party/miniz/miniz.c)
target_include_directories(miniz SYSTEM PUBLIC ../../third_party/miniz)
if(MSVC)
    target_compile_options(miniz PRIVATE /W3)
    target_compile_definitions(miniz PRIVATE _CRT_SECURE_NO_WARNINGS)
else()
    target_compile_options(miniz PRIVATE -Wall -Wextra)
endif()
add_subdirectory(../signal-core "${CMAKE_CURRENT_BINARY_DIR}/signal-core")
set(SPEC_SOURCE "${CMAKE_CURRENT_SOURCE_DIR}/../../runtime/UnRealDash/Source/DashboardSpec")
add_library(dashboard_spec STATIC
    ${SPEC_SOURCE}/Private/DocumentView.cpp
    ${SPEC_SOURCE}/Private/DefinitionPackBuilder.cpp
    ${SPEC_SOURCE}/Private/Errors.cpp ${SPEC_SOURCE}/Private/BoundedParse.cpp ${SPEC_SOURCE}/Private/SchemaValidation.cpp ${SPEC_SOURCE}/Private/SemanticPass.cpp
    ${SPEC_SOURCE}/Private/PackageReader.cpp ${SPEC_SOURCE}/Private/PathResolver.cpp ${SPEC_SOURCE}/Private/ImageHeader.cpp ${SPEC_SOURCE}/Private/RuleTreeBuilder.cpp)
target_include_directories(dashboard_spec PUBLIC "${SPEC_SOURCE}/Public")
target_include_directories(dashboard_spec SYSTEM PRIVATE ../../third_party/rapidjson/include ../../third_party/valijson/include)
target_link_libraries(dashboard_spec PUBLIC signal_core PRIVATE miniz)
target_compile_definitions(dashboard_spec PRIVATE VALIJSON_USE_EXCEPTIONS=0 RAPIDJSON_HAS_STDSTRING=1)
add_executable(dashboard-spec-validate ${SPEC_SOURCE}/Private/validate_main.cpp)
target_link_libraries(dashboard-spec-validate PRIVATE dashboard_spec)
file(GLOB test_sources CONFIGURE_DEPENDS tests/*.cpp)
add_executable(dashboard-spec-tests ${test_sources})
target_include_directories(dashboard-spec-tests SYSTEM PRIVATE ../../third_party/doctest)
target_link_libraries(dashboard-spec-tests PRIVATE dashboard_spec)
target_compile_definitions(dashboard-spec-tests PRIVATE DOCTEST_CONFIG_NO_EXCEPTIONS DOCTEST_CONFIG_NO_EXCEPTIONS_BUT_WITH_ALL_ASSERTS)
add_executable(telemetry-decode-jsonl tools/telemetry-decode-jsonl.cpp)
target_link_libraries(telemetry-decode-jsonl PRIVATE dashboard_spec)
foreach(target dashboard_spec dashboard-spec-validate dashboard-spec-tests telemetry-decode-jsonl)
    target_include_directories(${target} SYSTEM PRIVATE ../../third_party/rapidjson/include)
    target_compile_definitions(${target} PRIVATE RAPIDJSON_HAS_STDSTRING=1 DASHBOARD_SCHEMA_DIR="${CMAKE_CURRENT_SOURCE_DIR}/schema" REPO_ROOT="${CMAKE_CURRENT_SOURCE_DIR}/../..")
    if(MSVC)
        target_compile_options(${target} PRIVATE /EHs-c- /GR- /W4 /WX /permissive-)
        target_compile_definitions(${target} PRIVATE _HAS_EXCEPTIONS=0 _CRT_SECURE_NO_WARNINGS)
    else()
        target_compile_options(${target} PRIVATE -fno-exceptions -fno-rtti -Wall -Wextra -Wpedantic -Werror)
    endif()
endforeach()
message(STATUS "dashboard_spec: C++20, exceptions OFF, RTTI OFF, warnings as errors")
enable_testing()
foreach(source IN LISTS test_sources)
    get_filename_component(name "${source}" NAME)
    if(NOT name STREQUAL "main.cpp")
        add_test(NAME "${name}" COMMAND dashboard-spec-tests "--source-file=*${name}")
    endif()
endforeach()
