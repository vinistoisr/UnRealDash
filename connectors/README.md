# Connectors

Connector contracts and definition packs belong here (PLAN.md 2.9 and 3.7).
Transport, session, decoder and mapping remain separate. Runtime rendering,
owner XML parsing at runtime and platform implementations belong elsewhere.
